import { PublishDatePipe } from './publish-date.pipe';

describe('PublishDatePipe', () => {
  it('create an instance', () => {
    const pipe = new PublishDatePipe();
    expect(pipe).toBeTruthy();
  });
});
